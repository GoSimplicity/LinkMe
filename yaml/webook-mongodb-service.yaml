apiVersion: v1
kind: Service
metadata:
  labels:
    app: webook-record-mongo
  name: webook-record-mongo
spec:
  ports:
    - name: "27017"
      nodePort: 30884
      port: 27017
      protocol: TCP
      targetPort: 27017
  selector:
    app: webook-record-mongo
  type: NodePort
